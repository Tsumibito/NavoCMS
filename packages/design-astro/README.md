# @navocms/design-astro

The Astro adapter binds every component in a compiled design release to an explicit Astro module
and exposes recipe bindings and generated token CSS. Missing, duplicate, or unknown registrations
fail before an Astro build starts.
