# Generated design catalogue

This Astro application builds a review surface directly from the versioned Tidal Signal fixture.
It is a contract verification application, not a NavoCMS administration panel.

Run `pnpm check:catalogue` from the repository root to compile the design fixture, verify complete
Astro bindings, and generate the static catalogue.
